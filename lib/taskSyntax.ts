// Синтаксис, нужный конкретно для каждой задачи.
// Показывается на странице задачи всегда, отдельно от общей теории (relatedTheory).

export const taskSyntax: Record<number, string> = {
  1: `### Сигнатура
\`\`\`c
int is_even(int n);   // возвращает int: 1 или 0
\`\`\`

### Остаток от деления
\`\`\`c
int r = n % 2;    // % — остаток: 7 % 2 == 1, 8 % 2 == 0
\`\`\`

### Возврат значения
Функция с типом \`int\` обязана завершиться оператором \`return\`:
\`\`\`c
if (n % 2 == 0) {
    return 1;
} else {
    return 0;
}
\`\`\`

### Короткая форма
Выражение сравнения само даёт 1 или 0, поэтому можно вернуть его напрямую:
\`\`\`c
return n % 2 == 0;
\`\`\`

### Частые ошибки
- \`=\` присваивает, \`==\` сравнивает
- Отрицательные числа: \`-4 % 2 == 0\`, проверка на ноль работает и для них
`,

  2: `### Сигнатура
\`\`\`c
int max_of_three(int a, int b, int c);
\`\`\`

### Цепочка if / else if
\`\`\`c
if (a >= b && a >= c) {
    return a;
} else if (b >= c) {
    return b;
} else {
    return c;
}
\`\`\`

### Альтернатива: переменная-максимум
\`\`\`c
int max = a;
if (b > max) { max = b; }
if (c > max) { max = c; }
return max;
\`\`\`

### Частые ошибки
- \`>=\` вместо \`>\`, если числа могут совпадать (тест \`3 9 9\`)
- В каждой ветке нужен свой \`return\`
`,

  3: `### Сигнатура
\`\`\`c
int sign(int n);   // 1, 0 или -1
\`\`\`

### Три ветки
\`\`\`c
if (n > 0) {
    return 1;
} else if (n < 0) {
    return -1;
} else {
    return 0;
}
\`\`\`

### Отрицательное число в коде
Минус пишется слитно с числом: \`return -1;\`

### Частые ошибки
- Ветка для нуля обязательна: без \`return\` на каком-то пути функция вернёт мусор
`,

  4: `### Сигнатура
\`\`\`c
int absolute(int n);
\`\`\`

### Унарный минус меняет знак
\`\`\`c
int x = 5;
int y = -x;   // y == -5
\`\`\`

### Условие для модуля
\`\`\`c
if (n < 0) {
    return -n;
}
return n;
\`\`\`

### Частые ошибки
- Задача просит свою реализацию, без \`abs()\` из stdlib.h
- \`-2147483647\` в тестах есть: \`-n\` для него помещается в int
`,

  5: `### Сигнатура
\`\`\`c
int is_triangle(int a, int b, int c);   // 1 или 0
\`\`\`

### Неравенство треугольника
Каждая сторона меньше суммы двух других — нужны все три проверки сразу:
\`\`\`c
if (a + b > c && a + c > b && b + c > a) {
    return 1;
}
return 0;
\`\`\`

### Логическое И
\`&&\` истинно только когда истинны ОБЕ части, поэтому все три суммы обязательны.

### Частые ошибки
- Сравнение строгое: при \`a + b == c\` треугольник вырождается и не существует
`,

  6: `### Сигнатура
\`\`\`c
void grade_class(int score);   // void: функция ничего не возвращает, а печатает сама
\`\`\`

### Вывод строки
\`\`\`c
printf("отлично\\n");
\`\`\`

### Лесенка диапазонов
Границы проверяются сверху вниз — первая подходящая ветка и есть ответ:
\`\`\`c
if (score >= 90) {
    printf("отлично\\n");
} else if (score >= 70) {
    printf("хорошо\\n");
} else if (score >= 50) {
    printf("удовлетворительно\\n");
} else {
    printf("неудовлетворительно\\n");
}
\`\`\`

### Частые ошибки
- В \`void\`-функции нельзя писать \`return значение;\` (пустой \`return;\` допустим)
- \`\\n\` в конце строки: тесты сравнивают вывод построчно
`,

  7: `### Сигнатура
\`\`\`c
double euclidean_distance(double x1, double y1, double x2, double y2);
\`\`\`

### Тип double и квадратный корень
\`\`\`c
#include <math.h>          // уже подключён в шаблоне
double dx = x2 - x1;
double dy = y2 - y1;
double d = sqrt(dx * dx + dy * dy);
\`\`\`

### Возврат double
\`\`\`c
return d;
\`\`\`

### Частые ошибки
- \`sqrt\` требует \`<math.h>\`: без него результат может быть неверным
- Печать \`%f\` с шестью знаками делает скрытый main(), самому печатать не нужно
`,

  8: `### Сигнатура
\`\`\`c
int manhattan_distance(int x1, int y1, int x2, int y2);
\`\`\`

### Модуль разности своим if
\`\`\`c
int dx = x2 - x1;
if (dx < 0) { dx = -dx; }
\`\`\`

### Или готовой функцией abs
\`\`\`c
#include <stdlib.h>        // уже подключён в шаблоне
int m = abs(x2 - x1) + abs(y2 - y1);
\`\`\`

### Частые ошибки
- Модуль берётся у КАЖДОЙ разности отдельно, а не у их суммы
`,

  9: `### Сигнатура
\`\`\`c
double average_primes(int a, int b);
\`\`\`

### Цикл по отрезку включительно
\`\`\`c
for (int i = a; i <= b; i++) {
    // i пробегает все числа от a до b
}
\`\`\`

### Проверка простоты внутренним циклом
\`\`\`c
int is_prime = 1;
if (i < 2) { is_prime = 0; }
for (int d = 2; d * d <= i; d++) {
    if (i % d == 0) { is_prime = 0; break; }
}
\`\`\`

### Среднее как double
Деление целых даёт целое, поэтому нужен привод типов:
\`\`\`c
return (double)sum / count;    // без (double) выражение 17 / 4 даст 4
\`\`\`

### Частые ошибки
- Если простых нет (\`count == 0\`), делить нельзя — возвращайте \`0.0\`
- 1 не является простым числом
`,

  10: `### Сигнатура
\`\`\`c
int count_multiples(int a, int b, int k);
\`\`\`

### Счётчик в цикле
\`\`\`c
int count = 0;
for (int i = a; i <= b; i++) {
    if (i % k == 0) {
        count++;          // то же, что count = count + 1
    }
}
return count;
\`\`\`

### Частые ошибки
- Границы включительно: \`i <= b\`, а не \`i < b\`
- \`0 % k == 0\`: ноль кратен любому k и попадает в ответ (тест \`0 100 10\`)
`,

  11: `### Сигнатура
\`\`\`c
int is_leap_year(int year);   // 1 или 0
\`\`\`

### Составное условие
\`\`\`c
if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
    return 1;
}
return 0;
\`\`\`

### Короткая форма
Выражение сравнения само даёт 1 или 0:
\`\`\`c
return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
\`\`\`

### Приоритет операторов
\`&&\` связывает сильнее \`||\`, но скобки делают порядок явным и защищают от ошибки.

### Частые ошибки
- 1900 и 2100: делятся на 4 и на 100, но не на 400 → не високосные
`,

  12: `### Сигнатура
\`\`\`c
void season(int month);   // void: печатает сама
\`\`\`

### switch с группировкой case
Несколько case подряд без break попадают в один общий блок:
\`\`\`c
switch (month) {
    case 12:
    case 1:
    case 2:
        printf("зима\\n");
        break;
    case 3:
    case 4:
    case 5:
        printf("весна\\n");
        break;
    default:
        printf("некорректно\\n");
        break;
}
\`\`\`

### Частые ошибки
- Без \`break\` выполнение провалится в следующий case и допечатает лишнее
- \`default\` ловит 0, 13, 100 и отрицательные числа
`,

  13: `### Сигнатура
\`\`\`c
void day_of_week(int day);   // void: печатает сама
\`\`\`

### switch: свой case на каждое значение
\`\`\`c
switch (day) {
    case 1:
        printf("понедельник\\n");
        break;
    case 2:
        printf("вторник\\n");
        break;
    /* ... case 3 ... case 7 ... */
    default:
        printf("некорректно\\n");
        break;
}
\`\`\`

### Частые ошибки
- \`break\` после КАЖДОГО case
- Строки в printf — в двойных кавычках, \`\\n\` в конце
`,

  14: `### Сигнатура
\`\`\`c
int count_digits(int n);
\`\`\`

### do-while: тело выполняется хотя бы один раз
\`\`\`c
int count = 0;
do {
    count++;
    n = n / 10;        // отбросить последнюю цифру
} while (n != 0);
\`\`\`
Именно поэтому \`0\` даёт ответ 1: одна итерация проходит до первой проверки условия.

### Отделение последней цифры
\`\`\`c
int digit = n % 10;    // последняя цифра
n = n / 10;            // число без последней цифры
\`\`\`

### Отрицательные числа
Знак мешает, поэтому сначала берите модуль: \`n = abs(n);\` (stdlib.h подключён).

### Частые ошибки
- Точка с запятой после \`while (условие);\` обязательна — ею заканчивается оператор
`,

  15: `### Сигнатура
\`\`\`c
int reverse_number(int n);
\`\`\`

### Сборка числа наоборот
\`\`\`c
int reversed = 0;
do {
    reversed = reversed * 10 + n % 10;   // приписать последнюю цифру
    n = n / 10;                          // убрать её из n
} while (n != 0);
\`\`\`

### Сохранение знака
Знак сохраняется до цикла и возвращается в конце:
\`\`\`c
int sign = 1;
if (n < 0) { sign = -1; n = -n; }
/* ... цикл ... */
return sign * reversed;
\`\`\`

### Частые ошибки
- \`1000\` → \`1\`: ведущие нули в числе не хранятся, это правильное поведение
- Точка с запятой после \`while (...);\` обязательна
`,
};
